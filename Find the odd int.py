#We have an array of numbers, and one of them (and only one would appear an odd number of times).
#Create an array whose elements are themselves arrays containing 2 elements.
#Each of these sub arrays has index [0] containing the numbers, and index [1] containing the amount of times the number occurs. 
def find_it(seq):
    var1 = [None]*2    #seed array
    n = len(seq)
    var2 = [var1]*n
    for i in range(n):
        
    
